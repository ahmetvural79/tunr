# Go build output
/tunr
/dist/

# Binaries and test artifacts
*.exe
*.test
*.out

# Config and secret files — never commit these
*.env
.env.local
.env.*.local
secrets.json
*.key
*.pem
*.crt
*.p12

# tunr runtime files
/tmp/
/.tunr/
*.log
tunr.log

# macOS artist touches
.DS_Store
.AppleDouble
.LSOverride

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~
.vim/

# Coverage reports
coverage.txt
coverage.html
*.coverprofile

# Test binaries
**/*.test

# goreleaser output
/dist/
_build/

# ── PROPRIETARY / CLOSED SOURCE ─────────────────────────────────────────────
# The following are NOT part of the open source CLI.
# They live in private repositories.

# Internal documentation (kept locally, not published to GitHub)
docs/

# Relay server (proprietary infrastructure)

# Dashboard + landing page (proprietary — not published to GitHub)
landing/

# ── SECRETS / CREDENTIALS ────────────────────────────────────────────────────
# NEVER commit these. Ever.
supabase.json
paddle.json
.env
.env.*
.env.local
.env.*.local
*.secret

# Node.js (for SDK and dashboard)
node_modules/
.next/
.vercel/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
